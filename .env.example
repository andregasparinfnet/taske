# Database Configuration
POSTGRES_DB=taskedb
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Backend Configuration
SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD}
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# Network
BACKEND_PORT=8080
FRONTEND_PORT=80

# JWT Security Configuration (CRITICAL - CHANGE IN PRODUCTION!)
# Generate a secure key: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970

# Security Notes:
# 1. JWT_SECRET must be at least 64 characters long
# 2. Rotate JWT_SECRET periodically in production
# 3. Never commit actual .env file to version control
