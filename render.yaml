services:
  # 1. O Backend (Spring Boot)
  - type: web
    name: taske-backend
    env: docker
    rootDir: ./backend
    dockerfilePath: Dockerfile
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: 8080
      - key: DB_HOST
        fromDatabase:
          name: taske-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: taske-db
          property: port
      - key: DB_NAME
        fromDatabase:
          name: taske-db
          property: databaseName
      - key: DB_USER
        fromDatabase:
          name: taske-db
          property: user
      - key: DB_PASS
        fromDatabase:
          name: taske-db
          property: password

  # 2. O Frontend (React) - Configurado como Web Service Static
  - type: web
    name: taske-frontend
    runtime: static             # Define que é um Site Estático
    rootDir: ./frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist     # Pasta onde o Vite gera os arquivos
    # plan: free (Removed to fix validation error - Static sites default to free)
    # region: removed (Static sites are global)
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: taske-backend
          property: host

databases:
  # 3. O Banco de Dados
  - name: taske-db
    databaseName: taske
    user: taskeuser
    plan: free
    region: oregon
    ipAllowList: [] # Segurança: só permite acesso interno (do backend)
