# Test configuration
spring.main.allow-bean-definition-overriding=true

# Database for testing (H2 in-memory)
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# Increase rate limit for tests to avoid 429 errors during concurrent execution
rate.limit.capacity=100
rate.limit.refill-minutes=1

# Security headers (ensure they are enabled in tests)
server.ssl.enabled=false

# Cache
spring.cache.type=none

# JWT Configuration for tests
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.access-token-expiration=900000
jwt.refresh-token-expiration=604800000
jwt.issuer=taske-backend
jwt.audience=taske-frontend
jwt.clock-skew=60
